import { Styles } from './../types/styles';

const styles: Styles = {
   links: {
      display: 'flex',
      alignItems: 'center',
      gap: '15px',
      ml: 'auto',
      fontSize: '16px',
   }
}

export default styles
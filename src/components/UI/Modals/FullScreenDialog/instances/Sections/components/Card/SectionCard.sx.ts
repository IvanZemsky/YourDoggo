import { Styles } from "@/components/types/styles";
import { MainThemePalette } from "@/themes/MainTheme/options/MainThemePalette";

const styles: Styles = {
   card: {
      border: `3px solid ${MainThemePalette.primary.main}`,
      padding: '40px',
   }
};

export default styles;